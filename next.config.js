/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.squarespace-cdn.com",
      "i.dummyjson.com",
      "fonts.gstatic.com",
      "127.0.0.1",
    ],
  },
};

module.exports = nextConfig;
